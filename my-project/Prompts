Проверь, что длинны строчек кода не больше 80 символов и подразбей на более короткие строки.
 Не меняй ничего другого, логика не должна поменяться.

module AutomatShow(viewNKA,viewDKA) where

import Graphics.Gloss
import Types
import Data.List(intercalate)

-- ПАРАМЕТРЫ
radius :: Float
radius = 300

nodeR :: Float
nodeR = 45

-- отступ подписей переходов
labelLiftBase :: Float
labelLiftBase = 85

-- количество шагов для построения кривой
curveSteps :: Int
curveSteps = 50
...



Как нарисовать кривую в Gloss



Как убрать пересечения терминалов и состояний



Почему иногда терминал далеко от линии, а иногда близко?



Сделай цвет состояния чуть поярче



Если много состояний, то в центре терминалы накладываются. Как это исправить?



Почему линии заходят на состояния? Как сделатьсмещение на радиус окружности?



Как нарисовать дугу из состояния в себя же?



Получилась ломаная. Как исправить?

